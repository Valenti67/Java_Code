public class Teacher {
    // 目前，所有属性的定义全部带上 public
    // 属性的数据类型不限，可以是基本类型 OR 引用类型
    // 书写位置在类的内部 && 方法的外部
    // 定义规则：修饰符 变量类型 变量名 —— 这类变量（属性）
    // 属性也遵循默认值规则 —— 默认值都是 0 的变形
    public String name;
    public int age;
}
